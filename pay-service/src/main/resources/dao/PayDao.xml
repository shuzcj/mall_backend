<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.payservice.dao.PayDao">

    <insert id="createPayment" parameterType="com.mall.common.domain.entity.Payment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payments (order_id, user_id, amount, status, payment_date, created_at, updated_at)
        VALUES (#{orderId}, #{userId}, #{amount}, #{status}, #{paymentDate}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="getPaymentByOrderId" parameterType="int" resultType="com.mall.common.domain.entity.Payment">
        SELECT * FROM payments WHERE order_id = #{orderId}
    </select>

    <update id="updatePaymentStatus"  parameterType="com.mall.common.domain.entity.Payment">
        UPDATE payments
        SET status = #{status}, updated_at = #{updatedAt} ,payment_date = #{paymentDate}
        WHERE id = #{id}
    </update>

</mapper>

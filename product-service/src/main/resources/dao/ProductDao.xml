<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.productservice.dao.ProductDao">


    <insert id="addProduct" parameterType="com.mall.common.domain.entity.Product">
        INSERT INTO products
        (name, price, stock, image_urls,category_id,brand,description,sold,status,created_at,updated_at,user_id)
        VALUES
        (#{name}, #{price}, #{stock}, #{imageUrls},#{categoryId},#{brand},#{description},#{sold},#{status},#{createdAt},#{updatedAt},#{userId})
    </insert>

    <!-- Query Products -->
    <select id="getProducts" parameterType="map" resultType="com.mall.common.domain.entity.Product">
        SELECT *
        FROM products
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        <choose>
            <when test="sort == 'latest'">
                ORDER BY updated_at DESC
            </when>
            <when test="sort == 'topSales'">
                ORDER BY sold DESC
            </when>
            <when test="sort == 'asc'">
                ORDER BY price ASC
            </when>
            <when test="sort == 'desc'">
                ORDER BY price DESC
            </when>
        </choose>
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- Count Products -->
    <select id="countProducts" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM products
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <!-- Query Product By Id -->
    <select id="getProductById" parameterType="int" resultType="com.mall.common.domain.entity.Product">
        SELECT *
        FROM products
        WHERE id = #{id}
    </select>



    <!-- SQL to update stock by deducting -->
    <update id="deductStock">
        UPDATE products
        SET stock = stock - #{stock}
        WHERE id = #{productId} AND stock >= #{stock}
    </update>

    <!-- SQL to add stock -->
    <update id="addStock">
        UPDATE products
        SET stock = stock + #{stock}
        WHERE id = #{productId}
    </update>


    <!-- SQL to check current stock -->
    <select id="checkStock" resultType="int">
        SELECT stock FROM products WHERE id = #{productId}
    </select>


    <select id="getAllProducts" resultType="com.mall.common.domain.entity.Product">
        select * from  products

    </select>


    <update id="updateProduct" parameterType="com.mall.common.domain.entity.Product">
        UPDATE products
        <set>
            <if test="name != null"> name = #{name}, </if>
            <if test="price != null"> price = #{price}, </if>
            <if test="stock != null"> stock = #{stock}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="description != null"> description = #{description}, </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>

